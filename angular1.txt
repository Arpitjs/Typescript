Component

contains 1. template 2. class 3. metadata, decorator
template=> html
class => ts
metadata => information decorator

DECORATOR is a function that provides info about the metadata that provides info about the class attached to it

COMPONENT class contains data that is displayed in the view

metadata is an object thats used by the decorator to define class



ATTRIBUTE VS PROPERTY
attributes are defined by html but properties are defined by DOM


attributes initialize dom properties then they are done. their value cant be changed after initialization


PROPERTY BINDING
Binding to the html element using []

interpolation ie {{}} only works with string values